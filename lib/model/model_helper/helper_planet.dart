class Planet {
  String url;

  Planet(this.url);

  @override
  String toString() {
    return '{ ${this.url}}';
  }
}

class Spesies {
  String url;

  Spesies(this.url);

  @override
  String toString() {
    return '{ ${this.url}}';
  }
}

class Vehicle {
  String url;

  Vehicle(this.url);

  @override
  String toString() {
    return '{ ${this.url}}';
  }
}

class Character {
  String url;

  Character(this.url);

  @override
  String toString() {
    return '{ ${this.url}}';
  }
}

class StarShips {
  String url;

  StarShips(this.url);

  @override
  String toString() {
    return '{ ${this.url}}';
  }
}

class FilmList {
  String url;

  FilmList(this.url);

  @override
  String toString() {
    return '{ ${this.url}}';
  }
}
