import 'package:flutter/material.dart';



const JagoOutterMargin = const EdgeInsets.only(left: 17, right: 17, top: 16);

const JagoMargin = const EdgeInsets.only(left: 17, right: 17, top: 16);

const JagoMarginHorizontal = const EdgeInsets.only(left: 17, right: 17);

const JagoMarginBtn = const EdgeInsets.only(top: 12, bottom: 12);

const JagoDashboardPadding = const EdgeInsets.only(top: 16, bottom: 16);

const JagoOutterPadding = const EdgeInsets.only(top: 16, right: 24, left: 24);

const JagoHomePadding = const EdgeInsets.only(top: 10, right: 16, left: 16);

const JagoInnerPadding =
    const EdgeInsets.only(top: 16, right: 12, left: 12, bottom: 16);

const JagoSearchFieldPadding =
    const EdgeInsets.only(top: 0, right: 17, left: 17);

const JagoParagraph = const EdgeInsets.only(left: 8);

const JagoBoxedParagraph = const EdgeInsets.only(left: 8, top: 16, bottom: 16);

const JagoDialogPadding =
    const EdgeInsets.only(top: 30, right: 24, left: 24, bottom: 20);

const JagoSplash = const EdgeInsets.only(top: 75, left: 24, right: 24);

const JagoProfilePadding = const EdgeInsets.only(top: 30, right: 24, left: 24);

const JagoBoxPad = const EdgeInsets.only(top: 30, right: 24, left: 24);


